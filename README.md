# AviUtl2 フィルタ分離プラグイン
オブジェクトに付いているフィルタ効果群を個別オブジェクトに分離するAviUtl2 プラグインです。

## 導入方法
https://github.com/Garech-mas/AviUtl2-SplitFilters/releases/latest

上記のリンクにアクセスした後、`SplitFilters-v1.xx.zip` からダウンロードできます。

ZIPの中に入っている `SplitFilters.aux2` を、AviUtl2のプレビュー画面にドラッグ＆ドロップすることで導入できます。

For non-Japanese users, please drag and drop `SplitFilters.aux2` and `English.SplitFilters.aul2` onto the AviUtl2 preview window.

## 注意点
- 必ず最新バージョンのAviUtl2を導入してください。
<br>**必須バージョン以下の場合、プラグインが認識されない・使用できない場合があります。**

- **AviUtl (初代) では利用できません。** また、初代用「フィルタ分離」の作成予定もありません。

## 機能一覧
### フィルタ分離
- オブジェクトを右クリック → `プラグイン` → `フィルタ分離` で適用できます。
- オブジェクトに適用されているすべてのフィルタ効果を、ひとつのフィルタ効果/フィルタオブジェクトとして移し替えます。
<img width="359" height="146" alt="image" src="https://github.com/user-attachments/assets/c460c6e4-6c60-48b3-b2bc-3f4d7ca76961" />

### フィルタ分離（グループ制御）
- オブジェクトを右クリック → `プラグイン` → `フィルタ分離（グループ制御）` で適用できます。
- オブジェクトに適用されているすべてのフィルタ効果を、ひとつのグループ制御オブジェクトとして移し替えます。
<img width="366" height="147" alt="image" src="https://github.com/user-attachments/assets/165a4e05-ba40-4730-b545-355b9c3a4118" />

### 上のオブジェクトへフィルタ結合
- **フィルタ効果オブジェクトを**右クリック → `プラグイン` → `上のオブジェクトへフィルタ結合` で適用できます。
- フィルタ効果/フィルタオブジェクトに適用されているすべてのフィルタ効果を、直上のオブジェクトに結合します。
> [!WARNING]
> ※右クリックするオブジェクトはフィルタ効果/フィルタオブジェクトである必要があります。それ以外の場合、正常に動作できません。
<img width="363" height="154" alt="image" src="https://github.com/user-attachments/assets/2aea95c9-86e7-4f33-a749-7de5162f091e" />


## 更新履歴
### v1.00 (テスト済: beta22)
- 初版。

### v1.01 (テスト済: beta23a)
- 「上のオブジェクトへフィルタ結合」を追加。
- 必須バージョンをbeta23aに引き上げ、オブジェクト名が伝播してしまう問題を修正。
- バージョンチェックを追加。

### v1.02 (テスト済: beta23a)
- フィルタ結合時、特定の条件でクラッシュしてしまう問題を修正。
- フィルタ結合時、Layer1まで繰り返し探索するように変更。

### v1.03 (テスト済: beta31)
- 編集メニューへコマンドを追加し、ショートカットメニューの作成に対応。
- フィルタオブジェクトの分離・結合に対応。
- 各コマンドの失敗時にビープ音を鳴らすように変更。
- 「上のオブジェクトへフィルタ結合」失敗時、元のオブジェクトを作成し直すように変更。
- 英語用言語ファイルを追加。
